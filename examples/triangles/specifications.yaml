# - rootpath : [Path to the directory containing the source files for an implementation, relative to the path containing this .yaml file]
#   filepaths : [Path to the source files for an implementation, relative to its rootpath] 

- rootpath : implem1
  filepaths :
  - area.f90
  - triangles.f90

- rootpath : implem2
  filepaths :
  - area.f90
  - triangles.f90